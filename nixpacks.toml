[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = ["python oghie/manage.py collectstatic --noinput"]

[start]
cmd = "gunicorn oghie.wsgi --chdir oghie --workers 2 --bind 0.0.0.0:$PORT --log-file -"
